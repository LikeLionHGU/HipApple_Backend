# docker compose용 환경변수.
# 이 파일을 .env로 복사한 뒤 실제 값을 채우세요. (.env는 커밋되지 않습니다)
DB_PASSWORD=strong-mysql-password-here
JWT_SECRET=random-jwt-secret-at-least-32-characters-long
# Python AI 서버 주소 (같은 서버에서 8000 포트로 실행 중이면 기본값 그대로)
AI_SERVER_URL=http://host.docker.internal:8000
