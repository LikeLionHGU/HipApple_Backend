# 이 파일을 application-secret.yml로 복사한 뒤 실제 값을 채우세요.
# application-secret.yml은 .gitignore에 등록되어 있어 커밋되지 않습니다.
spring:
  datasource:
    username: root
    password: your-mysql-password

app:
  jwt:
    secret: your-random-jwt-secret-at-least-32-characters
