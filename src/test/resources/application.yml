spring:
  datasource:
    url: jdbc:h2:mem:lion_apple_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

google:
  oauth:
    client-id: test-google-client-id.apps.googleusercontent.com
    client-secret:

app:
  jwt:
    secret: test-jwt-secret-key-for-hipapple-backend-tests
    expiration-seconds: 86400
